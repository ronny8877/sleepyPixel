# Public site URL (optional — used for canonical URLs in production)
# PUBLIC_SITE_URL=https://sleepypixel.dev

# Cloudflare local secrets (not committed). Copy to .dev.vars for wrangler dev.
# See https://developers.cloudflare.com/workers/wrangler/configuration/environment-variables/
